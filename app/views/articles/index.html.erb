<head>
<style>
ol, ul {
    list-style: none;
}
li.grid {
  font: 13px "Helvetica Neue", Helvetica, Arial, sans-serif;
    float: left;
    width: 320px;
    height: 360px;
    padding: 10px;
    border: 5px solid gray;
    margin: 10px;
}

li.grid h2 {
  font-weight: bold;
  font-size: 20px;
    margin-bottom: 2px;
    max-width: 300px;
    overflow: hidden;
}

li.grid h3 {
  font-size: 11px;
  margin-top: 2px;
  color: #999;
  float: left;
  margin-right: 5px;
}

li.grid p {
    margin: 10px 15px;
    height: 265px;
    max-height: 240px;
    width: 300px;
    max-width: 300px;
    overflow: hidden;
}

.floating-box {
    float: left;
    max-width: 300px;
    margin: 0px 7px;
  }

</style>
</head>


<h1>Listing Articles</h1>
<div>
  <%= link_to 'New article', new_article_path %> | <%= link_to 'Go to Tags', tags_path %>
  <ol>

    <% @articles.each do |article| %>
      <li class="grid">
        <h2><%= article.title %></h2>
        <% article.tags.each do |tag| %>
          <h3><%= tag.name %></h3>
        <% end %>
        <p><%= article.text %></p>
        <ol>
          <li class="floating-box"><%= link_to 'Show', article_path(article) %></li>
          <li class="floating-box"><%= link_to 'Edit', edit_article_path(article) %></li>
          <li class="floating-box"><%= link_to 'Destroy', article_path(article),
                  method: :delete,
                  data: { confirm: 'Are you sure?' } %></li>
        </ol>
      </li>
    <% end %>
  </ol>
</div>
